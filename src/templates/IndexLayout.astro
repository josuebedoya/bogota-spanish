---
interface Props {
  route: string;
  lang: string;
}

const { route, lang } = Astro.props;

let Page;
if (route === "home") {
  Page = await import(`../pages/index.astro`);
} else {
  Page = await import(`../templates/${route}.astro`);
}
---

{lang}
<Page.default lang={lang} />
